---
interface Props { slug: string; label: string; count?: number; description?: string; }
const { slug, label, count, description } = Astro.props;
---
<a class="topic-chip" href={`/topics/${slug}/`} title={description}>
  {label}
  {count !== undefined && <span class="chip-count">{count}</span>}
</a>
