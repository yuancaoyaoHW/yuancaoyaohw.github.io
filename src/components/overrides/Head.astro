---
// 覆盖 Starlight 默认 Head 组件，注入可折叠侧边栏的 JS
const { head } = Astro.locals.starlightRoute;
---

{
  head.map(({ tag: Tag, attrs, content }) => (
    <Tag {...attrs} set:html={content} />
  ))
}
<script src="/scripts/collapsible-sidebars.js" defer></script>
