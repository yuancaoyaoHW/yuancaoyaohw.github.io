---
const books = [
  {
    title: 'Modern GPU Programming For MLSys',
    href: '/books/modern-gpu-programming-for-mlsys/',
    description: '面向机器学习系统的现代 GPU 编程中文翻译，包含章节正文、交互式示例和参考资料。',
    tag: 'GPU · MLSys',
  },
  {
    title: 'Convex Optimization',
    href: 'https://yuancaoyaohw.github.io/Convex-Optimization/',
    description: 'Boyd & Vandenberghe《Convex Optimization》中英对照翻译，含全书章节、原书插图与可拖动双栏布局。',
    tag: 'Math · Optimization',
  },
];

const blogPosts = [
  {
    title: 'Megakernel 与 Rubin：软件抽象与硬件底座',
    href: '/blog/event-tensor-megakernel/',
    description: '整理 megakernel（GPU 编程模型）与 Rubin（NVIDIA 下一代 GPU 架构）的互补关系，含 Event Tensor 原论文（MLSys 2026）Paper Parse 双模深读报告与知乎社区讨论。',
    tag: 'Megakernel · Rubin',
    date: '2026-07-22',
  },
  {
    title: 'Latent MoE 技术报告整理',
    href: '/blog/latent-moe/',
    description: '整理 NVIDIA LatentMoE 原论文 + Nemotron 3 应用 + 知乎社区讨论，含 Paper Parse 双模深读报告（Part A 专业解析 + Part B 核心逻辑提炼）和 MathJax 渲染。',
    tag: 'MoE · NVIDIA',
    date: '2026-07-21',
  },
  {
    title: 'DFlash 原理与 vLLM 适配实现',
    href: '/blog/dflash-vllm/',
    description: '从扩散模型到投机解码——DFlash 原理解析与 vLLM 代码落地。围绕两个核心问题展开：什么是扩散模型，以及为什么 draft 用扩散而 target 不用。含 MathJax 数学公式与 Mermaid 流程图。',
    tag: 'DFlash · vLLM',
    date: '2026-06-30',
  },
  {
    title: 'DeepSpec 与 DSpark 深度讲解',
    href: '/blog/deepspec-dspark/',
    description: 'Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation——含交互式 SVG 动画的技术分析报告，覆盖 DSpark / DFlash / Eagle3 三种算法与 Qwen3 / Gemma4 两大模型族。',
    tag: 'DeepSpec · DSpark',
    date: '2026-06-28',
  },
  {
    title: 'mHC 流形约束超连接论文详解',
    href: '/blog/mhc-hyper-connections/',
    description: 'Manifold-Constrained Hyper-Connections——含交互式动画的 DeepSeek 论文深度解读，让残差流既「宽」又「稳」。',
    tag: 'mHC · Hyper-Connections',
    date: '2026-06-27',
  },
];

const travelGuides = [
  {
    title: '稻城亚丁7天大环线·高德专用路书',
    href: '/travel/daocheng-yading-loop.html',
    description: '成都→康定→红海子→格底拉姆→新都桥→理塘→海子山→香格里拉镇(2晚)→稻城白塔→雅江→塔公/姑弄村/墨石→八美→康定→成都，1850km休闲环线。每段标注高德导航终点、加油站（必加满/建议补油）、连锁酒店精准地址，复制括号内文字直接粘贴高德搜索框即定位。坦克300全路段适配，车停即拍极少步行。',
    tag: '稻城 · 7D Loop',
    date: '2026-07-24',
  },
  {
    title: '川西滇北7日自驾路书',
    href: '/travel/chuanxi-dianbei-roadtrip.html',
    description: '成都→新都桥→稻城亚丁→梅里雪山→丽江→大理→昆明，1800km自驾全攻略。含Leaflet高德地图、每日风险评估（高反/雨季/加油断档）、3人舒适档预算、必吃清单与行前须知。',
    tag: '川西 · 7D Road Trip',
    date: '2026-07-23',
  },
  {
    title: '马来西亚 5 天旅游攻略',
    href: '/travel/malaysia-travel-guide.html',
    description: '三套方案（吉隆坡+槟城 / 吉隆坡+兰卡威 / 沙巴亚庇）完整攻略，含飞猪实时航班酒店搜索、高德地图路线、Accor 卡分析、小红书避雷、每日多套备选行程与预算明细。',
    tag: 'Malaysia · 5D4N',
    date: '2026-07-22',
  },
];
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>yuancaoyaohw</title>
    <meta name="description" content="yuancaoyaohw 的电子书与博客入口" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,200..800&family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --black: #080D10;
        --bg-1: #0B1117;
        --bg-2: #0E141C;
        --panel: #131A24;
        --panel-hover: #18212E;
        --white: #FFFFFF;
        --orange: #FF7A00;
        --orange-soft: rgba(255, 122, 0, 0.12);
        --text: #E7ECF3;
        --text-muted: #7C8699;
        --border: #1E2733;
        --border-hover: #2A3744;
        --radius: 10px;
        --font-display: "Bricolage Grotesque", Arial, sans-serif;
        --font-body: "Inter", system-ui, -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif;
      }

      * { box-sizing: border-box; margin: 0; padding: 0; }

      html { scroll-behavior: smooth; }

      body {
        background: var(--black);
        color: var(--text);
        font-family: var(--font-body);
        font-size: 16px;
        line-height: 1.6;
        -webkit-font-smoothing: antialiased;
        overflow-x: hidden;
      }

      a { color: inherit; text-decoration: none; }

      /* —— 全局环境光背景 —— */
      .bg-glow {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        background:
          radial-gradient(50% 40% at 85% 5%, rgba(255, 122, 0, 0.12) 0%, transparent 60%),
          radial-gradient(35% 25% at 20% 35%, rgba(255, 122, 0, 0.07) 0%, transparent 60%);
      }

      .bg-grid {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        background-image:
          linear-gradient(rgba(255, 255, 255, 0.025) 1px, transparent 1px),
          linear-gradient(90deg, rgba(255, 255, 255, 0.025) 1px, transparent 1px);
        background-size: 56px 56px;
        mask-image: radial-gradient(ellipse 90% 70% at 50% 20%, black 20%, transparent 75%);
        -webkit-mask-image: radial-gradient(ellipse 90% 70% at 50% 20%, black 20%, transparent 75%);
      }

      /* —— Nav —— */
      .nav {
        position: fixed;
        top: 0; left: 0; right: 0;
        z-index: 100;
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        background: rgba(8, 13, 16, 0.72);
        border-bottom: 1px solid var(--border);
        color: var(--white);
        padding: 16px 32px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-family: var(--font-display);
      }

      .nav .brand {
        font-size: 17px;
        font-weight: 600;
        letter-spacing: -0.02em;
        color: var(--white);
      }

      .nav .nav-links {
        display: flex;
        gap: 28px;
        align-items: center;
      }

      .nav .nav-links a {
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--text-muted);
        transition: color 0.2s;
      }

      .nav .nav-links a:hover { color: var(--white); }

      .nav .cta {
        border: 1px solid var(--border-hover);
        color: var(--white);
        padding: 8px 20px;
        border-radius: var(--radius);
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        background: transparent;
      }

      .nav .cta:hover { border-color: var(--orange); color: var(--orange); }

      /* —— 主内容容器 —— */
      .page {
        position: relative;
        z-index: 1;
      }

      /* —— Hero —— */
      .hero {
        padding: 180px 32px 100px;
        position: relative;
      }

      .hero-inner {
        max-width: 1000px;
        margin: 0 auto;
      }

      .hero .eyebrow {
        display: inline-block;
        font-family: var(--font-display);
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.14em;
        color: var(--orange);
        margin-bottom: 28px;
      }

      .hero h1 {
        font-family: var(--font-display);
        font-size: clamp(36px, 6.5vw, 72px);
        font-weight: 500;
        line-height: 1.1;
        letter-spacing: -0.03em;
        margin-bottom: 32px;
        color: var(--white);
      }

      .hero h1 .accent { color: var(--orange); }

      .hero .lede {
        max-width: 560px;
        font-size: 18px;
        line-height: 1.65;
        color: var(--text-muted);
      }

      /* —— Section 通用 —— */
      .section {
        padding: 72px 32px;
      }

      .section-inner {
        max-width: 1000px;
        margin: 0 auto;
      }

      .section-label {
        font-family: var(--font-display);
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.14em;
        color: var(--text-muted);
        margin-bottom: 36px;
        display: flex;
        align-items: center;
        gap: 12px;
      }

      .section-label::after {
        content: "";
        flex: 1;
        height: 1px;
        background: var(--border);
      }

      /* —— Book cards —— */
      .book-grid {
        display: flex;
        flex-direction: column;
        gap: 14px;
      }

      .book-card {
        background: var(--panel);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 24px 28px;
        display: flex;
        flex-direction: column;
        transition: transform 0.25s ease, border-color 0.25s ease, background 0.25s ease;
        position: relative;
        overflow: hidden;
      }

      .book-card::before {
        content: "";
        position: absolute;
        top: 0; left: 0; right: 0;
        height: 2px;
        background: linear-gradient(90deg, var(--orange), transparent);
        opacity: 0;
        transition: opacity 0.25s ease;
      }

      .book-card:hover {
        transform: translateY(-3px);
        border-color: var(--border-hover);
        background: var(--panel-hover);
      }

      .book-card:hover::before { opacity: 1; }

      .book-card .tag {
        align-self: flex-start;
        font-family: var(--font-display);
        font-size: 11px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: var(--orange);
        background: var(--orange-soft);
        padding: 5px 12px;
        border-radius: 999px;
        margin-bottom: 14px;
      }

      .book-card strong {
        font-family: var(--font-display);
        font-size: 19px;
        font-weight: 600;
        line-height: 1.25;
        letter-spacing: -0.01em;
        color: var(--white);
      }

      .book-card .description {
        margin-top: 10px;
        color: var(--text-muted);
        font-size: 14px;
        line-height: 1.55;
        flex: 1;
      }

      .book-card .go {
        margin-top: 24px;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-family: var(--font-display);
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--white);
      }

      .book-card .go svg {
        width: 16px; height: 16px;
        transition: transform 0.25s ease;
      }

      .book-card:hover .go svg { transform: translateX(4px); }
      .book-card:hover .go { color: var(--orange); }

      /* —— Blog posts —— */
      .post-list {
        display: flex;
        flex-direction: column;
        gap: 14px;
      }

      .post-card {
        display: flex;
        flex-direction: column;
        padding: 24px 28px;
        border: 1px solid var(--border);
        border-radius: var(--radius);
        background: var(--panel);
        transition: border-color 0.25s ease, background 0.25s ease, transform 0.25s ease;
      }

      .post-card:hover {
        transform: translateY(-2px);
        border-color: var(--border-hover);
        background: var(--panel-hover);
      }

      .post-card .tag {
        align-self: flex-start;
        font-family: var(--font-display);
        font-size: 11px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: var(--orange);
        background: var(--orange-soft);
        padding: 5px 12px;
        border-radius: 999px;
        margin-bottom: 14px;
      }

      .post-card strong {
        font-family: var(--font-display);
        font-size: 19px;
        font-weight: 600;
        line-height: 1.25;
        letter-spacing: -0.01em;
        color: var(--white);
      }

      .post-card .description {
        margin-top: 10px;
        color: var(--text-muted);
        font-size: 14px;
        line-height: 1.55;
      }

      .post-card .date {
        margin-top: 14px;
        font-family: var(--font-display);
        font-size: 12px;
        color: var(--text-muted);
      }

      .post-card:hover strong { color: var(--orange); }

      /* —— Footer —— */
      footer {
        padding: 40px 32px;
        text-align: center;
        font-size: 13px;
        font-family: var(--font-display);
        color: var(--text-muted);
        border-top: 1px solid var(--border);
      }

      footer a { color: var(--white); }
      footer a:hover { color: var(--orange); }

      /* —— 响应式 —— */
      @media (max-width: 640px) {
        .nav { padding: 14px 20px; }
        .nav .nav-links { gap: 16px; }
        .nav .nav-links a:not(.cta) { display: none; }
        .hero { padding: 130px 20px 72px; }
        .section { padding: 56px 20px; }
        .book-card { padding: 24px; min-height: auto; }
      }
    </style>
  </head>
  <body>
    <!-- 背景层 -->
    <div class="bg-grid"></div>
    <div class="bg-glow"></div>

    <div class="page">
      <!-- Nav -->
      <nav class="nav">
        <a href="/" class="brand">yuancaoyaohw</a>
        <div class="nav-links">
          <a href="#books">电子书</a>
          <a href="#posts">博客</a>
          <a href="#travel">旅游攻略</a>
          <a href="https://github.com/yuancaoyaoHW" class="cta">GitHub</a>
        </div>
      </nav>

      <!-- Hero -->
      <section class="hero">
        <div class="hero-inner">
          <span class="eyebrow">Blog · Translations · Infrastructure</span>
          <h1>AI Blog &amp; <span class="accent">Infra</span></h1>
          <p class="lede">AI 推理引擎、底层基础设施，以及相关的技术翻译与笔记。以可长期维护的文档站形式发布。</p>
        </div>
      </section>

      <!-- Books -->
      <section class="section" id="books">
        <div class="section-inner">
          <p class="section-label">电子书</p>
          <div class="book-grid">
            {
              books.map((book) => (
                <a class="book-card" href={book.href}>
                  <span class="tag">{book.tag}</span>
                  <strong>{book.title}</strong>
                  <span class="description">{book.description}</span>
                  <span class="go">
                    打开
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6" /></svg>
                  </span>
                </a>
              ))
            }
          </div>
        </div>
      </section>

      <!-- Blog Posts -->
      <section class="section" id="posts">
        <div class="section-inner">
          <p class="section-label">博客</p>
          <div class="post-list">
            {
              blogPosts.map((post) => (
                <a class="post-card" href={post.href}>
                  <span class="tag">{post.tag}</span>
                  <strong>{post.title}</strong>
                  <span class="description">{post.description}</span>
                  <span class="date">{post.date}</span>
                </a>
              ))
            }
          </div>
        </div>
      </section>

      <!-- Travel Guides -->
      <section class="section" id="travel">
        <div class="section-inner">
          <p class="section-label">旅游攻略</p>
          <div class="post-list">
            {
              travelGuides.map((guide) => (
                <a class="post-card" href={guide.href}>
                  <span class="tag">{guide.tag}</span>
                  <strong>{guide.title}</strong>
                  <span class="description">{guide.description}</span>
                  <span class="date">{guide.date}</span>
                </a>
              ))
            }
          </div>
        </div>
      </section>

      <!-- Footer -->
      <footer>
        <p>© {new Date().getFullYear()} yuancaoyaohw · 由 Astro 与 Starlight 驱动 · <a href="https://github.com/yuancaoyaoHW">GitHub</a></p>
      </footer>
    </div>
  </body>
</html>
